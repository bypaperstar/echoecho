# Demo 2 — grocery list + recipe search (scripted headless run; the
# recipe.search worker hits the LIVE keyless WP whitelist, then chains
# grocery.merge via generic follow_ups).
echo echo
help me plan dinners this week — I'm thinking pad thai one night
!dispatch_task {"kind": "recipe.search", "instructions": "pad thai"}
~wait 25
actually, drop the fish sauce and add coffee beans
!dispatch_task {"kind": "grocery.merge", "instructions": "drop the fish sauce and add coffee beans", "args": {"ingredients": ["coffee beans"]}}
~wait 3
that's it

# Mac extras on top of requirements.txt.
# Note: on macOS pin vosk<=0.3.44 (newest universal2 wheel).
-r requirements.txt
sounddevice==0.5.5
vosk<=0.3.44
# Native WebRTC Audio Processing Module (AEC and high-pass processing).
# Current release publishes arm64/x86_64 macOS wheels for Python >=3.9.
livekit==1.1.14
